{
    "id": "ea9d6bbb-6014-4d9d-867b-569f3b86e0c5",
    "name": "databricksDeployDBFSFiles",
    "friendlyName": "Databricks files to DBFS-TEST",
    "description": "Deploy files (py, jar, csv etc) to DBFS-TEST",
    "helpMarkDown": "",
    "category": "Deploy",
    "visibility": [
        "Build",
        "Release"
    ],
    "author": "Data Thirst Ltd",
    "version": {
        "Major": "{VersionMajor}",
        "Minor": "{VersionMinor}",
        "Patch": "{VersionPatch}"
    },
    "instanceNameFormat": "Databricks DBFS File Deployment",
    "inputs": [
        {
            "name": "bearerToken",
            "type": "string",
            "label": "Databricks bearer token",
            "defaultValue": "",
            "required": true,
            "helpMarkDown": "API Key - see your user account in your Databricks Workspace"
        },
        {
            "name": "region",
            "type": "string",
            "label": "Azure Region",
            "defaultValue": "westeurope",
            "required": true,
            "helpMarkDown": "The location of your workspace as seen in workspace URL"
        },
        {
            "name": "LocalRootFolder",
            "type": "string",
            "label": "Local Root Folder",
            "defaultValue": "",
            "required": true,
            "helpMarkDown": "Path to the folder to deploy for example $(System.DefaultWorkingDirectory)/drop"
        },
        {
            "name": "FilePattern",
            "type": "string",
            "label": "File Pattern",
            "defaultValue": "*.*",
            "required": true,
            "helpMarkDown": "File Pattern to match, example *.jar or MyFile.csv"
        },
        {
            "name": "TargetLocation",
            "type": "string",
            "label": "Target folder in DBFS",
            "defaultValue": "",
            "required": true,
            "helpMarkDown": "Target folder in DBFS, should start /, example /Folder/Subfolder"
        }
    ],
    "execution": {
        "PowerShell3": {
            "target": "vstsDeployDBFSFile.ps1",
            "platforms": [
                "windows"
            ]
        }
    }
}